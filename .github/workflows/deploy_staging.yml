name: Deploy to Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger deployment webhook
        run: |
          curl -X POST "${{ secrets.DEPLOY_WEBHOOK_URL_STAGING }}"
          echo "✓ Deployment webhook triggered successfully"
